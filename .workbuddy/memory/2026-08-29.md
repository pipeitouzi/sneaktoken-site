# 2026-08-29

## sneaktoken.com 海外英文站（从零搭建）

用户在腾讯云买了域名，要做面向海外客户的英文站，内容参考国内站 mianfeisuanli.com（免费大模型 API 额度库）。从零做出完整站点。

### 两个关键更正

1. **域名是 sneaktoken.com，不是 steaktoken**（用户首次口误）。sneak = 绕过付费墙，与"免费额度"主题天然契合 → 品牌叙事定为 "Sneak past the paywall"，比原本的 steaktoken 强很多。
2. **部署平台从 CloudBase 改为 Cloudflare Pages / Vercel**。我最初推荐 CloudBase 是错的：腾讯云上海环境绑定自定义域名走国内加速节点，会强制校验 ICP 备案，用户没备案会被卡死；且海外回源上海慢。已写入 DEPLOY.md。

**用户明确：sneaktoken.com 与 mianfeisuanli.com 完全独立，两套服务器，互不相干。**

### 站点产出

- `site/` 纯静态，无构建步骤。数据驱动：`data/offers.js` + `data/content.js`
- 结构：Hero + 筛选器（类型/条款/地区）+ 卡片网格 + 对比表 + How to pick + 方法论 + FAQ + 订阅 CTA
- 工具链：`tools/merge-offers.js` 把 `data/offers-*.json` 合并成 `site/data/offers.js`
- `DEPLOY.md`：CF Pages / Vercel 上线步骤 + DNSPod 解析表 + 检查清单

### 数据核实（本次最耗时、也最有价值的部分）

**结论：10 条，全部经官方页面确认，每条附源 URL。**

踩到的坑——搜索摘要和二手来源在这个话题上完全不可信：
- **Cerebras**：网上三个来源都说"无卡、每天 100 万 token"。抓官方文档发现 2026 年已取消无卡免费档，改为 $5 试用金 + 必须绑卡 + 30 天过期。
- **GitHub Models**：已于 2026-07-30 完全下线（官方原文 "fully retired"）。
- **Kaggle 网传"每周 30 小时免费 GPU"是误读**，那个数字属于付费 Colab 计划，免费档根本没公布周配额。
- **Lightning AI "每月 15 GPU 小时"** 官方已不再公布。
- **Together / RunPod / Vast.ai / Lambda / Baseten** 已无免费档。

第一个调研 agent 交付的 23 条数据**全部没有 source URL**，判定为搜索摘要拼凑，已隔离到 `data/unverified/`，不采用。

### 未完成 / 待办

- **数据缺口**：Google AI Studio 和 Google Colab 因 `ai.google.dev` / `research.google.com` 在大陆抓取失败（HTTP 000）未收录。这两条是海外用户最常用的，用户在海外网络下可自行补齐。
- 订阅表单只做了前端校验，未接邮件服务
- Footer 的 Privacy / Terms / Affiliate disclosure 仍是 `#` 占位——海外站（尤其有欧盟访客）这几页是合规必需
- 上线前需提交 Google Search Console（合规三页已做，不再是待办）

### FINAL：部署平台最终定为 Cloudflare Pages（同日收尾）

- 用户在 EdgeOne Pages 与 CloudBase 新加坡之间反复后，**最终选定 Cloudflare Pages**（Git 原生自动部署、全球 CDN、海外访问快）。
- 关键前提已澄清：**域名在腾讯云注册 + 网站放国外 = 兼容、免备案**。只需在 DNSPod 给 `sneaktoken.com` 加一条 CNAME 指向 Cloudflare 给的地址；注册地与托管地互不约束，ICP 备案只在"域名→国内服务器"时触发。
- 部署改为 Git 流程：项目已是 git 仓库（分支 `main`）。Cloudflare Pages 连 GitHub/GitLab → 输出目录 `site` → `git push` 自动上线。
- `DEPLOY.md` 已重写为 Cloudflare Pages 主流程；`site/_headers`（安全响应头）保留、`site/_redirects`（www→根 301）新增、`vercel.json` 删除（Vercel 专用）。
- 旧的 `sneaktoken-site.zip`（EdgeOne 用）已被 Git 忽略，部署走 Git 不再需要它。
- 用户需自备 GitHub/GitLab 账号（无对应连接器，我无法代建）；连好仓库 + 设输出目录 `site` + 绑域名即可。

- 为 Google AI Studio / Google Colab 写好待补模板：`data/templates/*.template.json`，核实要点见 `data/TBD.md`。模板文件名带 `.template`，不会被 merge 脚本匹配；用户补完数字后复制进 `data/offers-*.json` 再跑 `node tools/merge-offers.js` 即可。
- 用户确认部署走 Cloudflare Pages（已选定，见文末 FINAL）。

### 部署平台最终定为腾讯云 EdgeOne Pages（同日后续）

- 用户说"用腾讯云的海外站点部署" → 对应产品是 **腾讯云 EdgeOne Pages**（对标 Cloudflare Pages）。加速区域选"全球可用区（不含中国大陆）"即可免备案，DNS 在 DNSPod 不用挪，自动签免费 SSL。比 Cloudflare Pages 更顺（控制台在国、DNS 不切）。
- 已重写 `DEPLOY.md` 为 EdgeOne Pages 路径，并打了部署包 `sneaktoken-site.zip`（用 Python zipfile 打包，排除 `_headers`/`vercel.json`，因为 CF/Vercel 专属文件在 EdgeOne 用不上）。
- 注：没有现成的 EdgeOne Pages MCP 工具可直接部署，需用户在腾讯云控制台手动上传 zip 或连 Git 仓库。

### 合规三页已生成（同日后续）

- 用户问"TODO/unknown 填什么、是不是公开前必须挂" → 澄清：模板里的 unknown/TODO **不会**上线（不在 merge 范围），属于可选的 Colab/AI Studio 数据补全；真正公开前必须挂的是 Footer 的合规三页。
- 已生成 `site/privacy.html` / `site/terms.html` / `site/disclosure.html`（英文模板，带 `[REPLACE: ...]` 占位标出必填的运营实体/邮箱/生效日期），并把 Footer 的 `#` 指向这三页。
- 用户要求**化繁为简**：网站初级、不涉及付费，但一定要合规和安全。已据反馈简化：
  - 合规三页砍掉"运营实体/地址/管辖"硬占位，改为极简版，只需填联系邮箱 + 生效日期；明确声明"无付费、无返利、非官方"。
  - 卡片外链原带 `rel="...sponsored"` 是错的（暗示返利），已改为 `noopener noreferrer nofollow`（不涉及付费就不应标 sponsored，noreferrer 防 tabnabbing）。
  - 订阅表单本就 `e.preventDefault()` 不上传，提示文案从"已本地保存"改为诚实的"demo form"。
  - 经验：用户偏好"合规安全但不复杂"——合规页给最小可用版即可，别堆法务占位。
